{
  "name": "todo_app",
  "version": "1.0.0",
  "description": "full stack todo app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "cd todoApp && yarn install",
    "dev": "cd todoApp && yarn dev",
    "build": "cd todoApp && yarn build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.3"
  }
}
